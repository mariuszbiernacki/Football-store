<!-- Wpisz w input i naciśnij napisz. -->
<!--  -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Domowe 00</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto+Mono&amp;subset=latin-ext" rel="stylesheet">


    <style>
        div {
            font-family: roboto;
            max-width: 80%;
            margin: 20px auto;
            font-size: 80px;
            letter-spacing: 1px;
        }

        span.cursor {
            opacity: 0;
            transition: .2s;
        }

        span.typing {
            opacity: 1
        }
    </style>
</head>

<body>
    <div>
        <span class="text"></span><span class="cursor"> |</span>
    </div>
    <script>

        const spnTxt = document.querySelector(".text")
        const spnCursor = document.querySelector(".cursor")

        const cursorAnimation = () => {
            spnCursor.classList.toggle("typing") // toggle przelacza z 1 klasy na druga
        }
        let indexCursor = setInterval(cursorAnimation, 400)

        const txt = ["hej", "siema", "witam", "xyz", "oooo", "uuuuuu"]


        let indexWord = 0
        let indexLetter = 0;


        let changeWord = () => {
            indexWord++
            indexLetter = 0
            indexStart++
            spnTxt.innerHTML = ""
            if (indexWord == txt.length - 1) {
                clearInterval(indexChange)
            }
            setInterval(addLetter, 200)

        }

        let indexChange = setInterval(changeWord, 3000)


        let addLetter = () => {


            spnTxt.innerHTML += txt[indexWord][indexLetter]
            indexLetter++;
            if (indexLetter == txt[indexWord].length) {
                clearInterval(indexStart)
            }

        }
        let indexStart = setInterval(addLetter, 200)